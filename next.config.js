/** @type {import('next').NextConfig} */
const nextConfig = {
  // 이미지 최적화 설정
  images: {
    domains: [],
  },
  // 환경 변수 설정
  env: {
    // 추후 API 키 등 환경 변수 추가
  },
}

module.exports = nextConfig


